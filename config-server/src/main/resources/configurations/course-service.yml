spring:
  application:
    name: course-service
  data:
    redis:
      host: localhost
      port: 6380
      database: 0
  kafka:
    producer:
      bootstrap-servers: 172.19.94.160:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:5432/course_db
    username: postgres.nbctaqdagentcyufrrjk
    password: tdoHj9KNcKUr1RDr
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: public

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080